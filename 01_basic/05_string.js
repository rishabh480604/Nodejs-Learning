const name="hello"
const age=15

//to print 
console.log("my name is "+name+" and age is "+age);
//other way
console.log(`my name is ${name} and age is ${age}`);

const gameName=new String('rishabh');// as an object same as java

//us e all functions
console.log(gameName.length);
console.log(gameName[0]);

const data="name surname"
data=data.replace(' ','_')
console.log(data);

//similarly work on url also
console.log(data.includes('name'));//to that contains or not

console.log(data.split('_'));
//to split from points
